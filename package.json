{
  "name": "quickappointment-workspace",
  "private": true,
  "version": "1.0.0",
  "description": "Workspace scripts for QuickAppointment frontend and backend",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "lint": "npm --prefix backend run check && npm --prefix frontend run lint",
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix backend run start"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
